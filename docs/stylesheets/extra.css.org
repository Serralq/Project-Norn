* Build( extra.css )
:PROPERTIES:
:header-args: :comments link :mkdirp yes :tangle extra.css
:END:
** Misc
- variables
- headers
- unordered lists symbol
- link hover color
- max width
- elements to disappear when on mobile
#+BEGIN_SRC css
.twemoji {
  --md-icon-size: 1.3em !important;
}

/* Headers should have equal margins
 ,* Thus h1 should not have special treatment */
h1 {
  margin-bottom: 0px !important;
}

/* Add underline under headers */
h1:after,
h2:after,
h3:after,
h4:after,
h5:after,
h6:after {
  content: " ";
  display: block;
  border: 1px solid var(--md-typeset-color);
}

/* Use dashed unordered lists */
.md-content li:not(.task-list-item)::marker {
  content: "- ";
  font-size: 1.2em;
}

/* For hovering over url links */
/* Since this is html element I made */
/* only for index page since index link base color is different */
.custom_link:hover, .md-footer__button svg  {
  color: var(--md-primary-fg-color) !important;
}

/* For total size of entire website */
.md-grid {
  max-width: 1360px;
}

/* Max width breakpoints */
/* To remove the pixel dust of landing page welcome */
@media (max-width: 512px) {
    .mobile {
        display: none;
    }
}
#+END_SRC
** TODO Link cards
- Add images once it feels right
#+BEGIN_SRC css
/* Everyone has nicer borders */
.note, .card-link {
  border-width: 2px !important;
}
/* Linked cards */
.card-link {
  color: var(--md-typeset-color) !important;
  background-color: #1E2738;
  /* Allow for border inside of element */
  border: 5px solid #1E2738 !important;
  box-sizing: border-box;
  box-shadow: inset 0px 0px 0px 2px #448aff;

  /* For if there is a background image */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  transition:
    background-color 0.25s,
    color 125ms,
    border 0.25s,
    background-image 1.25s,
    box-shadow 0.25s !important;
}
/* Smaller grids */
.grid-smaller {
  grid-template-columns: repeat(
    auto-fit,
    minmax(min(100%, 12rem), 1fr)
  ) !important;
}

#+END_SRC
*** Custom link color classes
#+BEGIN_SRC css
/* Special colors for hovering */
.card-link:hover {
  border: 3px solid #448aff !important;
}
.card-link.deep-purple:hover {
  background-image: url('../img/tech.gif');
  border: 3px solid #7e56c2 !important;
}
.card-link.brown:hover {
  background-color: #795649 !important;
}
.card-link.green:hover {
  background-image: url('../img/travel.gif');
  border: 3px solid  #4cae4f !important;
}
.card-link.blue-grey:hover {
  background-image: url('../img/resume.png');
  border: 3px solid #546d78 !important;
}
/* These two colors came from mkdocs-material button background color */
/* Not the same as the actual class name anymore */
.card-link.light-blue:hover {
  background-image: url('../img/jellyfin.png');
  border: 3px solid #42a5f5 !important;
}
.card-link.amber:hover {
  background-image: url('../img/copyparty.png');
  border: 3px solid #ff8a66 !important;
}
.card-link.red:hover {
  background-image: url('../img/immich.png');
  border: 3px solid #ff1947 !important;
}
.card-link.pink:hover {
  background-image: url('../img/doom.png');
  border: 3px solid #e92063 !important;
}
#+END_SRC
*** TODO Custom banner images
- Add more banner images
#+BEGIN_SRC css
/* Custom Banner Images */
/* Only remove normal banner once left side bar appears */
@media screen and (min-width: 1220px) {
  /* Current top tab */
  .md-tabs__item--active {
    background: var(--md-primary-fg-color);
    border: 2px solid #1E2738 !important;
    box-sizing: border-box;
    box-shadow: inset 0px 0px 0px 2px #448aff;
  }
  .md-tabs__link {
    /* Magic numnber to center the tabs */
    margin-top: 12px;
    align-items: center;
  }
  /* Remove once showing custom top */
  .md-header {
    display: none;
  }
  /* The top banner */
  .md-tabs {
    background-image: url('../img/banner.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    height: 120px;
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
  }
  .md-tabs::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 20px;
    background: linear-gradient(to bottom, transparent, var(--md-default-bg-color));
  }
  /* Align to the right */
  .md-tabs .md-grid {
    width: 100%;
    display: flex;
    justify-content: flex-end;
  }
}
#+END_SRC
** Footer Social Box
#+BEGIN_SRC css
.md-footer {
    /* In between the blog article and content */
    background-color: color-mix(in srgb, var(--md-footer-bg-color), var(--md-default-bg-color));
    z-index: 20; /* Stops the sidebar from overlapping */
}
/* The actual footer social card */
.md-footer-meta {
    background-color: var(--md-footer-bg-color);
}
.custom-footer-grid {
    align-items: center;
    justify-content: center;
    min-height: 100vh; /* Fills the entire viewport */
}
#+END_SRC
*** Footer Author
#+BEGIN_SRC css
.footer-author {
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: 1px;
    padding-left: 20px; 
    
    /* Prepping for the purple box */
    position: relative; 
    line-height: 1.2; 
}

/* The Purple Box */
.footer-author::before {
    content: "";
    position: absolute;
    
    /* Position: Aligned to bottom */
    bottom: 18px;
    left: -30px;
    
    width: 105px;
    height: 18px;
    
    background-color: var(--md-primary-fg-color);
    opacity: 0.3;
    z-index: 1; /* Above the text */
}
#+END_SRC
*** Footer Contents
#+BEGIN_SRC css
/* Centered Line Separator */
.footer-separator {
    width: 60px;
    height: 2px;
    background-color: var(--md-footer-fg-color);
    opacity: 0.3;
    margin: 1.5rem 0;
}

.footer-socials {
    display: flex;
    gap: 30px;
}

/* --- 2. Links with Animated Underline --- */
.footer-link {
    color: white !important;
    transition: opacity 0.3s, transform 0.2s;
    opacity: 0.8;
    display: flex;
    position: relative;     /* Required for positioning the ::after element */
    padding-bottom: 8px;    /* Make space for the box to appear */
}

/* The Sliding Box (Hidden by default) */
.footer-link::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0%;
    height: 4px;
    background-color: var(--md-primary-fg-color); 
    transition: width 0.3s ease-out;
}

/* Trigger animation on hover */
.footer-link:hover::after {
    width: 100%;
}
.footer-link:hover {
    opacity: 1;
}

/* Icons */
.footer-link .twemoji, .footer-link .twemoji svg {
    height: 1.5rem;
    width: 1.5rem;
}

/* Wrapper to group text content and left-align it */
.footer-content-wrapper {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    max-width: 100%;
}

.footer-label {
    min-width: 100px;
}
#+END_SRC
** Currently Reading Hover Effect
#+BEGIN_SRC css
.reading-card-img {
    width: 100%;
    transform: scale(0.9);
    transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    border-radius: 4px;
}
.reading-card-link:hover .reading-card-img {
    transform: scale(1);
}
#+END_SRC
